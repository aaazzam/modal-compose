Write a file to the sandbox filesystem.

Usage:
- `file_path` must be absolute.
- Existing files are overwritten.
- Parent directories must already exist.
- Prefer `edit` when changing an existing file.
