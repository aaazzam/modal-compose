[project]
name = "modal-compose"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Adam Azzam", email = "33043305+aaazzam@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anyio>=4.14.1",
    "modal>=1.5.1",
    "pydantic>=2.13.4",
]

[project.scripts]
modal-compose = "modal_compose:main"

[build-system]
requires = ["uv_build>=0.11.17,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-cov>=6.0",
]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
    "--import-mode=importlib",
    "--cov=modal_compose",
    "--cov-report=term-missing",
    "--cov-report=html:htmlcov",
]
filterwarnings = [
    "error",
]
markers = [
    "unit: fast, isolated, no external dependencies (the default).",
    "integration: touches Modal / network / external layers.",
    "slow: noticeably slow; deselect with -m 'not slow'.",
]

[tool.coverage.run]
branch = true
source = ["modal_compose"]
omit = ["*/examples.py"]

[tool.coverage.report]
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "\\.\\.\\.",
]

[tool.pyright]
include = ["src"]
typeCheckingMode = "strict"
pythonVersion = "3.12"
reportMissingTypeStubs = false
